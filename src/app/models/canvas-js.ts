import { InterfaceLibrary } from "./InterfaceLibrary";
//import CanvasJS from 'canvasjs' 

export class CanvasJs implements InterfaceLibrary{

    draw( json: any, c: any ): void {
      /*
        var theme = json.theme;
        var animationEnabled = json.animationEnabled;
        var exportEnabled = json.exportEnabled;
        var title = json.title.text;
        var type = json.data.type;
        var showInLegend = json.data.showInLegend;
        var toolTipContent = json.data.toolTipContent;
        var indexLabel = json.data.indexLabel;
        var dataPoints = json.data.dataPoints;
    
        const chart: any = new CanvasJS(c, {
            theme: theme,
            animationEnabled: animationEnabled,
            exportEnabled: exportEnabled,
            title:{
              text: title
            },
            data: [{
              type: type,
              showInLegend: showInLegend,
              toolTipContent: toolTipContent,
              indexLabel: indexLabel,
              dataPoints: dataPoints
              }]
        });
        */
    }
    
}
